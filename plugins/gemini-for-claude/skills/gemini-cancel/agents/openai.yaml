interface:
  display_name: "Gemini Job Cancel"
  short_description: "Cancel a tracked Gemini for Claude job"
  brand_color: "#4F46E5"
  default_prompt: "Use Gemini for Claude to cancel a tracked Gemini job only if the runtime reports safe cancellation."
